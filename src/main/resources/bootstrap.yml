spring:
  application:
    name: configserver 
  cloud:
    config:
      server:
        git:
          uri: ssh://git@innersource.accenture.com/app_ap/config-properties.git
  rabbitmq:
   addresses: 10.0.0.35:5671
   host: configmq
   port: 5671
   username: guest
   password: guest
server:
  port: 8888